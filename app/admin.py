from app.models import Appointment, Blog, essentials
from django.contrib import admin

# Register your models here.
admin.site.register(Appointment)
admin.site.register(Blog)
admin.site.register(essentials)